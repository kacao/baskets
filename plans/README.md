# Implementation Plans

Generated by the `improve` skill on **2026-07-12**, planned against commit **`3958dd6`**.
Each plan is self-contained and executable by a separate, zero-context agent (`execute <plan>`).

Execute in the order below unless a dependency says otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, run every verification command, and
update your row in the table when done.

> **Planning-time caveat (read before any drift check):** the working tree had **two
> uncommitted files** at planning time — `src/lib/server/tasks.ts` and
> `src/lib/components/TaskPanel.svelte` (an in-progress "sub-tasks inherit parent milestone"
> feature). Because the planned-at SHA `3958dd6` **equals HEAD**, `git diff --stat 3958dd6..HEAD`
> shows nothing even though the tree differs. Plans touching those two files say so; when in
> doubt, **match the "Current state" excerpts by code content, not line number.** The feature
> does not overlap any recurrence/move/delete/permission region the plans change.

## Execution order & status

| Plan | Title                                                    | Priority | Effort | Risk    | Depends on | Status                                                         |
| ---- | -------------------------------------------------------- | -------- | ------ | ------- | ---------- | -------------------------------------------------------------- |
| 001  | Gate integration tests behind a flag (kill skip-as-pass) | P1       | S      | LOW     | —          | DONE                                                           |
| 002  | Add indexes on hot foreign-key columns                   | P1       | M      | LOW     | —          | DONE                                                           |
| 003  | Spawn recurrence on board drag-to-done                   | P1       | S      | LOW     | —          | DONE                                                           |
| 004  | Add CI pipeline (check + unit; flag-gated integ/e2e)     | P1       | M      | LOW     | 001        | DONE                                                           |
| 005  | Collapse the bulk-permission N+1                         | P1       | S      | LOW     | —          | DONE                                                           |
| 006  | Validate `Origin` on the WebSocket upgrade               | P1       | S      | LOW     | —          | DONE                                                           |
| 007  | Scope `person` custom-field values to project members    | P1       | S      | LOW     | —          | DONE                                                           |
| 008  | Unlink file bytes on task/project delete                 | P2       | M      | MED     | —          | DONE (integration test ran green)                              |
| 009  | Map-based resolvers for `cfSearchByTask`                 | P2       | S      | LOW     | —          | DONE                                                           |
| 010  | Keyed maps for TableView per-row/cell lookups            | P2       | M      | MED     | —          | DONE (manual cell-reactivity QA pending)                       |
| 011  | Wrap multi-write service ops in transactions             | P2       | M      | MED     | —          | DONE (tx-atomicity integration test passing)                   |
| 012  | Add ESLint + Prettier + editorconfig                     | P2       | S–M    | MED     | —          | DONE (0 err / 135 warn backlog)                                |
| 013  | Document project grants + statuses in `llms.txt`         | P2       | S      | LOW     | —          | DONE                                                           |
| 018  | One-command `verify` script + drop webwright scratch     | P2       | S      | LOW     | —          | DONE                                                           |
| 014  | Service-layer + permissions unit tests (test-DB harness) | P3       | L      | LOW–MED | —          | DONE (`test:server` script; not yet in CI)                     |
| 015  | Component tests + coverage instrumentation               | P3       | M–L    | LOW     | —          | DONE (`*.svelte.test.ts` runs via `npm test`, not `test:unit`) |
| 016  | Consolidate duplicated view-component logic              | P3       | L      | MED     | 014 (rec.) | DONE (ADR-055 pane behavior covered by e2e)                    |
| 017  | Coordinated toolchain major bumps                        | P3       | M      | MED     | 004 (rec.) | DONE                                                           |
| 019  | Investigate splitting the god files (server-load slice)  | P3       | L      | MED     | 014 (rec.) | DONE (Step 1 shipped; Steps 2–3 = recommendations)             |
| 020  | Investigate recurrence double-spawn race                 | P3       | S      | LOW     | 011        | DONE — NOT REPRODUCED (repro test kept as guard)               |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (one-line reason) | `REJECTED` (one-line rationale).

**Recommended first batch** (all S/M, low-risk, high-leverage): **001, 002, 003, 005, 006, 007** —
close the test blind spot, index the hot columns, fix the board-drag recurrence bug, kill the
bulk N+1, and shut the two cross-origin/cross-project security gaps. Add **004** in the same pass
for the CI gate (it depends on 001).

## Animation plans (021-031, from the improve-animations audit @ e3e25a4)

| #   | Plan                                                                                   | Severity | Status |
| --- | -------------------------------------------------------------------------------------- | -------- | ------ |
| 021 | [Easing tokens + motion hygiene](021-motion-foundation-tokens.md)                      | MEDIUM   | DONE   |
| 022 | [Remove constant-frequency animations](022-frequency-discipline-deletions.md)          | HIGH     | DONE   |
| 023 | [Menu entrance cohesion](023-menu-entrance-cohesion.md)                                | MEDIUM   | DONE   |
| 024 | [Popover scale-from-trigger](024-popover-scale-from-trigger.md)                        | LOW      | DONE   |
| 025 | [Board drag release settle](025-board-drag-release-settle.md)                          | MEDIUM   | DONE   |
| 026 | [sortable.ts indicator skip](026-sortable-indicator-skip.md)                           | LOW      | DONE   |
| 027 | [Tooltip warm window](027-tooltip-warm-window.md)                                      | MEDIUM   | DONE   |
| 028 | [Progress fills → scaleX](028-progress-fills-scalex.md)                                | LOW      | DONE   |
| 029 | [Toaster bidirectional transition](029-toaster-bidirectional.md)                       | LOW      | DONE   |
| 030 | [Chevron rotate](030-chevron-rotate.md)                                                | LOW      | DONE   |
| 031 | [Additive polish (bulk bar / onboarding / done-color / press)](031-additive-polish.md) | LOW      | DONE   |

**Execution order**: 021 → 022 → 023 → 024, then 025-031 in any order (run sequentially — several
share files). Hard deps: 023/024/030/031 reference the `--ease-*` tokens and `DUR_*` constants 021
introduces (each has a stated fallback if run standalone); 024 assumes 022 already stripped the
Popover/StatusSelect transitions; 023 step 5's import cleanup assumes 022 ran first.
Rejected during planning (do not re-audit): sortable release snap-back settle (manual DOM
reordering inside Svelte-keyed lists — fragile; documented in 026); ConfirmModal "asymmetric press
timing" (code already matches the catalog's press target); popover flip-direction fix (moot once
022 removes the only flipping surface's transition); pageFade re-litigating (documented deliberate).
All 11 executed + review-fixed on 2026-07-15. Post-execution review fixes folded in: tooltip
click-cool (pointerdown resets the warm window), BoardView pendingMove label-lane correctness +
object-identity clear, mobile `.filter-pop` transform-origin flip, TableView sub-row chevron
rotate, BulkActionBar `fly|global` (Svelte 5 transitions are local by default and the page mounts
the component conditionally). **Follow-up (not planned)**: the task-pane collapsible chevrons still
glyph-swap (TaskPanel.svelte Description/Sub-tasks/cf-task heads, CustomFieldValue.svelte
multi-field head) — their toggles contain text labels, so the rotate needs an icon-scoped wrapper,
unlike the icon-only view toggles. Also noted during execution (fixed): a pre-existing BoardView
bug — `onDrop` read `over` after `reset()` nulled it, so board drops never fired `?/moveTask`.

## Dependency notes

- **004 → 001** (hard): CI must not run the integration suite while it still passes green with no
  server. Fix the skip-as-pass gating (001) before wiring integration/e2e into CI (004).
- **020 → 011** (hard): the double-spawn race is closed by the transaction mechanism 011 introduces.
- **016 → 014** (soft, recommended): characterization tests (014) de-risk the view refactor.
- **019 → 014** (soft, recommended): same — extract the server-load helpers under test.
- **017 → 004** (soft, recommended): land CI first so the major-version bumps are gated by the
  full test matrix automatically.
- **008 & 011** both touch `src/lib/server/tasks.ts` deletion/mutation paths — if landing both,
  do 011 (transactions) first so 008's multi-site delete-then-unlink can run inside one.

## Investigate-only plans

**019** and **020** are _investigate_ plans (reproduce/scope first, then decide), not
fix-now plans — per the audit's LOW/MED confidence. 019's only pre-approved slice is the
low-risk server-load helper extraction.

## Direction options — NOT planned (product decisions for the maintainer)

The audit also surfaced five forward-looking options. These are **not** in the plan set (they're
calls for the maintainer, and selected ones would become _design/spike_ plans, not build-everything
plans). Recorded here so they aren't re-discovered as "findings":

- **A. Generic outgoing webhooks + broader event catalog** — `dispatchEvent` is already the one
  chokepoint (3 events → Slack only); PRD-named. Design the payload/signing contract first.
- **B. REST parity for project import** — `importProjectFromExport()` exists (`projectIO.ts`) with
  no `/api/projects/import` route; breaks the PRD's "script drives the full lifecycle via REST."
- **C. API pagination + rate limiting** — list endpoints return unbounded arrays; PRD flags both
  as the gate before any public deployment.
- **D. Custom fields v2 (sort/group/filter by field)** — v1 is display-only by design; the
  one-TEXT-column storage makes typed comparison the hard part.
- **E. Read-only permission tier** — `canAccessProject ≡ canEditProject` today; deferred as a
  _product decision_, not a bug.

Say "plan A" (etc.) to turn any into a design/spike plan.

## Findings considered and rejected

Recorded so they aren't re-audited next run:

- **`postgres` + `better-sqlite3` as "duplicate deps"** — rejected: intentional dual-dialect setup (ADR-050).
- **`heroicons` vs `iconoir:` tokens as "duplicate icon library"** — rejected: `iconoir:` is a
  back-compat namespace prefix over Heroicon names (ADR-052); `iconoir` isn't a dependency.
- **RichText / `linkify` XSS** — rejected: hrefs are fixed to `http(s)`/`mailto`, no `{@html}`; safe.
- **`npm audit` advisories** — none: 0 vulnerabilities at planning time.
- **Indexes on `taskCustomValue.taskId` / `taskLabel.taskId`** — rejected: each is already the
  leading column of its composite PK, so already indexed (folded into 002's explicit exclusions).
- **`@testing-library/svelte` as an unused-dep removal** — rejected as a standalone: folded into
  015 (keep it and write the first component test that uses it).
- **ADR-settled tradeoffs (NOT findings):** invalidate-and-refetch realtime (last-write-wins);
  404-not-403 for inaccessible reads; un-throttled `bsk_` bearer keys; no read-only permission tier
  (deferred product decision); push-based schema / no migration files; one-TEXT-column custom
  values; single-process `server.js`; and the three ADR-049-deferred client extractions (viewConfig
  mutators, the rollup-resolver closure, view-tab drag→`use:sortable`) — plan 016/019 explicitly
  steer around these.
